import { Button } from "@/components/ui/button"
import { Card } from "@/components/ui/card"

export const GenerarXML = () => {
    return (
        <Card className="w-full bg-white ">
            <Button className="bg-green-500 text-white">Generar xml</Button>
        </Card>
    )
}