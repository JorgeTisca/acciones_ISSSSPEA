{
  "extends": "next/core-web-vitals",
  "excludedFiles": "components/buscador/",
  "overrides": [
    {
      "files": ["bin/*.js", "lib/*.js"],
      "excludedFiles": "components/buscador/",
      "rules": {
        "quotes": ["error", "single", "type"],
        "no-unused-vars": "off", // Desactiva la regla de variables no utilizadas
        "@typescript-eslint/no-unused-vars": "off", // Desactiva la regla de variables no utilizadas en TypeScript
        "@typescript-eslint/no-explicit-any": "off", // Desactiva la regla que prohíbe el uso de `any`
        "semi": ["error", "always"] // Mantén otras reglas que necesites
      }
    }
  ]
}
